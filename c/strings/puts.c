#include <stdio.h>

int main(){
    char str[30];
    gets(str);     //takes value and prints it 
    puts(str);     //prints the value and puts cursor on the next line


    printf("MF");
    return 0;
}