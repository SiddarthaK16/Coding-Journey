#include <stdio.h>

int main()
{

    int marks[10];
    marks[0] = 89;
    marks[1] = 28;  //marks[89] is the end

    printf("The value of marks 0 and marks 1 is %d and %d respectively.", marks[0], marks[1]);
    return 0;
}